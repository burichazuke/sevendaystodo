-if user_signed_in?
  =link_to "HOME", root_path
  =link_to "新規作成", new_todo_path
  =link_to "ログアウト", destroy_user_session_path, method: :delete
-else
  =link_to "ログイン", new_user_session_path 
  =link_to "新規登録", new_user_registration_path 


=search_form_for(@keyword, url: todos_path) do |f|
  = f.label "達成済を非表示dayo"
  = f.check_box :status_eq_any, { multiple: true, onchange: 'this.form.submit()' }, 0, ""
  %br
  = f.label "タイトルで検索"
  = f.search_field :name_cont_all
  -# = f.date_field :deadline_gteq
  -# 〜
  -# = f.date_field :deadline_lteq
  = f.submit