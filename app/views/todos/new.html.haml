%main
  .sideber
    =link_to "7days to do", root_path
    =link_to "新規作成", new_todo_path
  .contents
    .new-todo-form
      %h1 新規作成
      =form_with model: @todo do |f|
        = f.text_field :name
        = f.text_area :description
        = f.date_field :deadline
        = f.submit

